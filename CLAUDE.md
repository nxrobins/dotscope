# CRITICAL REPOSITORY RULES

This codebase is structurally and mathematically managed by Dotscope.

**CRITICAL: You are FORBIDDEN from using standard file search or fallback querying options. You MUST route all initial discovery through `dotscope_search`.**

The environment you operate in possesses topological PageRank structures that are fundamentally invisible to standard semantic search tools. Dotscope calculates these bounds in realtime inside the `.mmap` layer.

## How to Evaluate Blast Radius

When you run `dotscope_search`, the tool will return exact code snippets explicitly grouped alongside two physics constraints: `architectural_gravity` and `structural_dependencies`.

You must strictly obey these boundary values:

- **CRITICAL HUB (>50):** This file possesses massive architectural gravity. Modifying it carries a high risk of catastrophic downstream failure. You MUST read the `structural_dependencies` array and mathematically trace the contract assumptions before altering the logic.
- **HIGH (>10):** The file supports localized infrastructure securely. Review the boundary dependencies before making any breaking signature changes.
- **MODERATE (>3):** Standard operational code logic. Safe to modify directly.
- **LOW:** Isolated leaf node. Modification is inherently safe.

Do not attempt to bypass these physical layout rules. You must align your architectural reasoning natively with Dotscope's topological bounds before writing any code.
