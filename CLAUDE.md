# CRITICAL REPOSITORY RULES: THE PHYSICS ENGINE

This codebase is structurally and mathematically managed by Dotscope. Standard semantic search is lexically blind and strictly forbidden unless investigating isolated strings.

**CRITICAL: You are FORBIDDEN from modifying any architectural structure without first routing discovery through `resolve_scope`.**

The code you operate in possesses physical structures that are fundamentally invisible to standard search. Dotscope compiles these bounds in realtime via a native Rust `petgraph` daemon.

## How to Evaluate Blast Radius

When you run `resolve_scope`, Dotscope intercepts the query and dynamically calculates physical constraints. It will explicitly inject warnings into your JSON-RPC payloads.

You must strictly obey these boundaries:

- **[DOTSCOPE_GRAVITY_WARNING] (>50):** CRITICAL HUB DETECTED. This file possesses massive architectural gravity. Modifying it carries a high risk of catastrophic downstream failure. You MUST mathematically trace the structural inputs before altering the logic.
- **[DOTSCOPE_GRAVITY_NOTE] (>10):** High dependency density. Silently verify downstream imports in the structural array before modifying logic.
- **STANDARD:** Standard operational code logic. Safe to modify directly.

Do not attempt to bypass these physical layout rules. You must align your architectural reasoning natively with Dotscope's topological bounds before writing any code.
